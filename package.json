{
  "name": "y",
  "version": "1.0.0",
  "description": "git init\r npm init -y\r npm i colors cors dotenv express express-async-handler mongoose\r npm i --save-dev nodemon\r md config,controllers,models,routes\r =============================================\r index.js\r ---------\r const express = require('express');\r const cors = require('cors');\r const colors = require('colors');\r const dotenv = require('dotenv').config();\r const app = express();\r const port = process.env.PORT || '5000';",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
